# Notes 2026-02-03

- Cloned schema-only DB to `4erp_main` and pointed app config to it.
- Seeded core data for admin access (roles/admin user), doc numbers, and permissions.
- Fixed item code uniqueness by appending `-1/-2` and added vehicle serial handling in Items.
- Polished UI: Jobs list balance, Roles header alignment, dashboard-config data seeding.
- Added permission-gated dashboard widgets (required_permissions) and seeded base role permissions for dashboard config.
- Fixed dashboard-config save/reset audit logging and added schema patch for required_permissions.
- Dashboard now gates all widgets by permissions/config; added dispatch/system health widgets and updated sidebar permission gating.
- Job actions now additionally respect RBAC permissions (UI + backend) while still enforcing status/role lockpoints.
- Added supplier type support (Goods/Service/Manpower) with DB patch; PO create modal now sets supplier type (auto for PR Manpower) and supplier create API stores it.
- Fixed PO cancel action: submit button now posts with reason and cancel permission enforces PO_VOID for Draft/Submitted.
- PO create: for Manpower, unit selection now supports วัน/เดือน with helper hints and dynamic switching based on PO type.
- PO manpower: added duration/unit fields in po_items, updated PO create calculation to use คน × (วัน/เดือน), and adjusted PO view/manpower UI to show proper units.
- Sidebar: added robust active-state detection so current menu highlights correctly across modules and subpages.
- Added HR Manpower PO module listing approved manpower POs, plus sidebar entry for HR/Admin to access manpower registration.
- PO manpower registration now aligns rate/unit with PO (day/month) in new/existing registration forms and display.
- PO manpower unit inference now falls back to po_items.unit when manpower_unit is missing, so old POs show correct /วัน or /เดือน.
- Seeded missing doc_number_settings types (docs + EMP/EXT + item codes) and switched People creation (HRM, master, PO manpower) to DocumentNumber sequence.
- Items now use DocumentNumber for DEV/EQP/VEH/CON codes; item code field is read-only and auto-generated.
- Added PO/GR void workflow (schema patch + UI/status handling), including manpower void/transfer options and GR reversal safeguards.
