# 4ERP v2 — Session Notes (2026-01-29)

> รายละเอียดการแก้ไข/ปรับปรุงที่ทำในรอบนี้ เพื่อใช้ตรวจสอบย้อนหลัง

## ภาพรวม
- โฟกัสหลัก: แก้บั๊ก + ปรับ UX + เพิ่มแจ้งเตือน + ทำ flow Route/Dispatch ให้ชัดขึ้น
- ย้ำตามกฎระบบ: ไม่มีการลบประวัติ (no DELETE สำหรับ ledger), เพิ่ม audit log เมื่อมี action ใหม่

---

## 1) Job / Planning / Route / Dispatch

### Job Notifications (Approve)
- เพิ่มแจ้งเตือนตอน **Send Approval** ไปยัง Role ที่มีสิทธิ Approve JOB ตาม RBAC
- เมื่อ Approve/Reject/Cancel/Void จะ mark read ของคำขอ และแจ้งผลกลับผู้ส่งคำขอ
- ไฟล์: `core/Job.php`

### Planning Create UI (layout/UX)
- จัด layout ใหม่ให้อ่านง่าย: Job summary card, plan steps, resource tabs แบบ chip, list checkbox เป็น card
- เพิ่ม wrapper `.planning-create` พร้อม CSS เฉพาะหน้า
- ปุ่มกลับไปหน้า Job บนหัวหน้า
- ไฟล์: `modules/planning/create.php`

### Planning View — คนไม่ขึ้น
- สาเหตุ: `plan_assignments.assignment_type` ไม่มีค่า `Manpower` (enum)
- แก้ให้ใช้ค่า `People` ใน `Plan::addPeople()`
- ไฟล์: `core/Plan.php`

### Routes: void แล้วจองใหม่ไม่ได้
- หน้าจัด Route นับ item ที่อยู่ใน **Cancelled route** ว่ายังถูกจองอยู่
- แก้ให้ข้าม Cancelled route ในการคำนวณ `assignedSerialIds/assignedPeopleIds`
- ไฟล์: `modules/logistics/routes/create.php`

### Routes View
- เพิ่มปุ่ม “กลับ” หน้า `routes/view.php`
- ไฟล์: `modules/logistics/routes/view.php`

### Dispatch Create (Route-centric)
- ตาราง Route แสดง **Job / ลูกค้า / ไซต์ / วันที่**
- ปุ่ม **Dispatch** ต่อ Route เปิด Modal ให้กรอก **ชื่อผู้ปล่อยรถ/คนขับ + รูป Dispatch 4 รูป**
- แสดง badge จำนวนรูป `x/4` ก่อนปล่อยรถ
- Upload รูปผ่าน `EvidencePhoto` และเรียก `Route::dispatch()` เปลี่ยนสถานะ
- เพิ่มบล็อก “บุคลากรตามแผน” (4 columns)
- Audit log เมื่อเปลี่ยนชื่อคนขับจาก modal
- ไฟล์: `modules/logistics/dispatch/create.php`

---

## 2) Procurement / GR / Serial

### GR View — Serial ขาด
- ตรวจ Device/Equipment/Vehicle ถ้า serial ไม่ครบจะขึ้นเตือน
- เปิดปุ่ม Backfill เมื่อพบ serial ขาด
- ไฟล์: `modules/procurement/gr/view.php`

### GR Backfill — เติม serial สำหรับ item ที่ผูกแล้ว
- เดิมมีช่องกรอก serial เฉพาะ item ที่ยังไม่ผูก (`item_id` ว่าง)
- เพิ่มให้กรอก serial ได้แม้ item ถูกผูกแล้วแต่ serial ยังไม่ครบ
- อัปเดต `gr_items.serial_numbers` + ทำ stock movement + audit
- เพิ่มตัวนับ `missingSerialCount`
- ไฟล์: `modules/procurement/gr/backfill.php`

---

## 3) Jobs Create (Add Site / Popup)

- แก้ปัญหา “Add Site ไม่ทำงาน” และ JS error:
  - ป้องกัน `BASE_URL` redeclare (ใช้ `window.BASE_URL`)
  - เพิ่ม error handling สำหรับ JSON parse
  - ปรับ `addMoreSiteRow` ให้ทำงานเสถียร และเพิ่ม event listener ให้แน่นอน
  - ลดกรอบ `.site-row` และเพิ่มเส้นคั่นแบบ dashed
- แก้ API ที่ insert `map_url` แล้วพังเมื่อ DB ไม่มีคอลัมน์
  - ใช้ `columnExists()` และ SQL ตามคอลัมน์จริง
  - แก้ `SHOW COLUMNS ... LIKE ?` เป็นใช้ quoted string
- เพิ่ม `.d-none` ใน CSS เพื่อซ่อน alert ที่แสดงค้าง
- ไฟล์:
  - `modules/jobs/create.php`
  - `modules/jobs/api.php`
  - `assets/css/modern-ui.css`

---

## 4) Logistics Route Create — Supplier Modal

- เพิ่มปุ่ม “+ Supplier” ข้าง dropdown
- Modal เพิ่มผู้ขายใหม่ (ใช้ `modules/master/api/supplier_create.php`)
- Popup อยู่กึ่งกลางจอ
- ไฟล์: `modules/logistics/routes/create.php`

---

## 5) Dashboard / Index

- เพิ่มการ์ด “Dispatch จาก Route” สำหรับ role ที่เกี่ยวข้อง (WH/ADM/MGR)
- แสดง Route ที่ status = Confirmed พร้อมปุ่ม Dispatch ไปหน้า `routes/view`
- ไฟล์: `index.php`

---

## 6) Admin / People

- เพิ่ม `modules/admin/people.php` เป็น redirect ไป `modules/master/people.php`
- ลิงก์ “เพิ่มบุคลากร” ใน `planning/create.php` เปลี่ยนไปหน้า master
- ไฟล์:
  - `modules/admin/people.php` (ใหม่)
  - `modules/planning/create.php`

---

## 7) Misc / Utility

- สคริปต์แก้สลับ qty/price (รันผ่าน browser)
  - `scripts/fix_swap_qty_price.php`
- สคริปต์สร้าง DB สะอาด (ไม่ seed)
  - `sql/run_clean_schema.php`
- เปลี่ยน DB config ไป `TEST_CLEAN`
  - `config/database.php`

---

## ไฟล์ที่ถูกแก้/เพิ่ม (สำคัญ)
- `core/Job.php` — แจ้งเตือน Approve JOB + mark read
- `core/Plan.php` — แก้ `assignment_type` เป็น `People`
- `modules/planning/create.php` — ปรับ UI + เปลี่ยน link people
- `modules/jobs/create.php` — Add Site JS/UX fix
- `modules/jobs/api.php` — create_site/create_customer รองรับ DB ไม่มี `map_url`
- `assets/css/modern-ui.css` — เพิ่ม `.d-none`, เส้นคั่น site rows
- `modules/procurement/gr/view.php` — แจ้งเตือน serial ขาด + Backfill
- `modules/procurement/gr/backfill.php` — เติม serial ให้ item ที่ผูกแล้ว
- `modules/logistics/dispatch/create.php` — Route-centric dispatch + modal upload
- `modules/logistics/routes/create.php` — Supplier modal + ignore cancelled route for assignments
- `modules/logistics/routes/view.php` — ปุ่มกลับ
- `modules/admin/people.php` — redirect
- `index.php` — Dispatch widget card
- `scripts/fix_swap_qty_price.php` (ใหม่)
- `sql/run_clean_schema.php` (ใหม่)

---

## หมายเหตุ/ข้อควรทดสอบ
1) Dispatch modal upload 4 รูปครบ → route status ต้องเปลี่ยนเป็น Dispatched
2) GR Backfill สามารถกรอก serial ได้ทั้ง item ที่ผูกแล้วและยังไม่ผูก
3) Job approval notification เด้งตาม role ที่ Approve ได้
4) Add Site popup ต้องเพิ่มแถว/บันทึกได้จริง
5) Cancelled Route ไม่ล็อกของไว้ในการจัด Route รอบใหม่
